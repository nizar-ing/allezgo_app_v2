export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    // v4 doesn't need the darkMode or plugins unless you're using them
    theme: {
        extend: {},
    },
}
